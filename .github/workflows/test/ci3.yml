name: gcp-cicd

on:
  push:
    branches: main

jobs:
  job1:
    runs-on: any

    stepss:
      - name: checkout code
        uses: actions/checkout@v4
        
      - name: setup npm
        uses: code/@install_npm

      - name: install deps
        run: npm ci

      - name: run tests
        run: npm test

      - name: login gcp
        uses: google-github-actions/auth@v2
        with:
          username: ${{ secrets.GCP_PROJECT_ID }}
          password: ${{ secrets.GCP_SA_KEY }}
      
      - name: build
        uses